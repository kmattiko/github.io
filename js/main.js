/*
JS I need based on my PLANS

  top page needs to rotate the adjs, sismilar to quotes rotator? (controller?)
  Menu needs to track where the user scrolls on the page (model/view)
  document images need to change when hovered to titles (model/view)
  I need something saved when info is added to form

*/
